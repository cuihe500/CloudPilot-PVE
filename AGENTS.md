# CloudPilot-PVE AI 开发约束

本仓库的核心编码工作由 AI 完成。本文件是所有 AI 编码工具进入仓库后的首要项目级指令。

## 1. 事实来源

发生冲突时按以下顺序处理：

1. 用户当前明确指令。
2. 已批准的 OpenSpec 变更及验收场景。
3. [`docs/architecture.md`](docs/architecture.md) 中的架构与安全边界。
4. `api/openapi.yaml` 中的前后端接口契约。
5. 已有代码、测试和其他文档。

不要静默猜测冲突的意图；指出冲突并请求确认。实现改变了行为时，必须同步对应规范和文档。

## 2. 开发流程

每项非简单改动都按以下顺序执行：

1. **理解**：阅读相关 OpenSpec、完整调用链、相邻实现和测试。
2. **约束**：确认权限、配额、审批、审计、幂等和失败恢复要求。
3. **实现**：复用现有代码，使用标准库或已安装依赖，提交最小完整改动。
4. **验证**：运行能证明改动正确的最小检查，再运行仓库统一检查。
5. **复核**：检查 diff 是否包含越界修改、生成文件误改、秘密信息或未同步契约。
6. **报告**：只报告实际运行过的检查及其结果；未验证的内容必须明确说明。

复杂功能、跨组件行为或需求变化必须先进入 OpenSpec。格式化、拼写和不改变行为的小修正可直接处理。

## 3. Git 分支与 PR

所有变更，包括文档和紧急修复，都必须通过独立分支和 Pull Request 进入 `main`：

1. 确认工作树干净，并将本地 `main` 快进同步到 `origin/main`。
2. 从最新 `main` 创建新分支；禁止直接在 `main` 开发或提交。
3. 一个分支只承载一个 OpenSpec 变更或一个内聚的小任务。
4. 实现、验证和只读审查完成后再提交；提交信息使用简洁的 Conventional Commits 风格。
5. 推送当前分支并创建目标为 `main` 的 PR。
6. PR 必须按模板写明背景、实际修改、验证证据、风险、契约/迁移影响和未完成项；不能只写提交标题。
7. 评审修正继续提交到同一分支，重新运行受影响检查并更新 PR 描述。
8. CI 和必要人工评审通过前不得合并；禁止强制推送和绕过分支保护。

建议分支名：`feat/<topic>`、`fix/<topic>`、`docs/<topic>`、`chore/<topic>`。任务的默认交付物是可审查的分支、提交和 PR，而不是直接修改 `main`。

## 4. 不可突破的架构边界

- LLM 只将自然语言转换为结构化资源请求，不负责授权和执行。
- LLM 不得获得 PVE 凭据，不得直接调用 PVE，不得执行任意 Shell。
- 权限、配额、容量、模板、网络和有效期校验必须由确定性 Go 代码完成。
- 用户审批必须绑定不可变的变更计划版本或摘要；计划变化后必须重新审批。
- 所有 PVE 写操作必须通过后台持久化任务执行，并具备幂等键、超时、重试上限和审计记录。
- HTTP 请求生命周期内不得直接完成长时间 PVE 操作，也不得仅依赖 goroutine 保存任务状态。
- 浏览器不得直连 PVE、数据库或模型服务。
- 第一版保持单体和单集群，不引入微服务、Kafka、Kubernetes、插件平台或多模型抽象。

完整说明见 [`docs/architecture.md`](docs/architecture.md)。

## 5. 编码规则

### Go

- 默认使用 `net/http`、`encoding/json`、`context`、`log/slog` 等标准库。
- 数据访问使用显式 SQL；事务边界必须清晰。
- 只有存在两个真实实现或明确测试替身需求时才定义接口。
- 所有外部调用必须传递 `context`，设置超时，并为错误补充操作上下文。
- 不记录令牌、密码、Cloud-init 密钥、完整模型提示或其他秘密。
- 已发布数据库迁移不可修改，只能追加新迁移。

### TypeScript / Vue

- API 类型从 OpenAPI 生成，不手写重复 DTO，不直接修改生成文件。
- 服务端数据优先保留在页面或 composable；出现真实跨页面需求后才增加全局状态。
- MVP 使用轮询更新执行状态；没有明确需求时不增加 WebSocket。
- 保持可访问性基础：语义化元素、键盘操作、表单标签和明确错误提示。

### 通用

- 不做顺手重构，不为假设中的未来需求建立抽象。
- 不新增依赖来替代几行可靠的标准库或平台能力。
- 修复根因和共享入口，不在每个调用方重复打补丁。
- 信任边界的输入必须验证；不能以“保持代码简单”为由省略安全检查。
- 任何安全相关默认值都必须失败关闭，而不是失败放行。

## 6. 高风险代码的最低测试要求

以下逻辑必须有自动化测试：

- RBAC 与资源归属。
- 配额和容量边界。
- 审批状态转换及计划摘要绑定。
- 幂等执行、失败重试和任务恢复。
- 到期回收和防误删条件。
- PVE 请求构造及 UPID 状态处理。
- LLM 结构化输出的拒绝路径。

PVE 集成测试使用假 HTTP 服务，不允许测试连接生产 PVE。前端至少保留一条“申请 → 预览 → 审批 → 执行状态 → 审计”的端到端黄金路径。

## 7. 验证约定

项目初始化完成后，根目录 `Makefile` 必须提供：

```text
make generate
make test
make check
make build
```

`make check` 是提交前统一入口。当前命令尚不存在或环境不具备条件时，不得声称验证通过；应列出实际执行的替代检查和缺口。

推荐在完成前调用项目技能：

- `/skill:systematic-debugging`：出现错误、失败或异常行为时。
- `/skill:verification-before-completion`：准备声称完成、提交或发起合并前。
- `/review`：对当前 diff 做只读审查。
- `/verify`：执行并记录完成前验证。

## 8. 人工确认点

AI 可以自主实现已批准范围内的代码，但以下操作必须由人确认：

- 修改认证、授权、审批或配额语义。
- 执行破坏性数据库迁移。
- 连接或修改真实 PVE 环境。
- 降低 TLS、CSRF、会话、审计或凭据保护要求。
- 强制推送、删除分支、发布版本或部署生产环境。
- 偏离已批准 OpenSpec 或本文件中的安全边界。

## 9. 完成定义

只有同时满足以下条件才能称为完成：

- 已批准验收场景得到满足。
- 相关测试、类型检查和构建已实际运行并通过。
- OpenAPI、数据库迁移和生成代码保持同步。
- 没有未解释的安全、数据丢失或回滚风险。
- 文档反映最终行为。
- 变更已提交到非 `main` 分支，并创建内容完整、目标为 `main` 的 PR。
- 最终报告包含分支、提交、PR 链接、验证命令、结果和仍存在的限制。
